apiVersion: v1
kind: Service
metadata:
  name: cinem-app
  labels:
    app: cinem-app
spec:
  type: ClusterIP
  selector:
    app: cinem-app
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
