apiVersion: v1
kind: Service
metadata:
  name: cinem-app
  labels:
    app: cinem-app
spec:
  type: NodePort
  selector:
    app: cinem-app
  ports:
    - name: http
      port: 80
      targetPort: 3000
      nodePort: 30080
      protocol: TCP
